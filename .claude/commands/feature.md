Add a new feature card to the "Core Features" section with the following specification:

$ARGUMENTS

Requirements:
1. Create an appropriate icon component (SVG, stroke-based, matching existing style)
2. Follow the existing Feature component pattern
3. Use consistent styling with border-zinc-800/50 and emerald-500 hover states
4. Keep description concise (2 sentences max)
5. Add the new Feature to the grid in the proper section
6. Ensure responsive grid behavior is maintained

After implementation:
- Run `npm run lint` to verify code quality
- Check the feature visually in dev mode
- Verify hover animations work smoothly
